/**
 * @file  common.h
 * @brief C header file for common functions.
 */

#pragma once

#include <stdbool.h>

char* string_duplicate(const char* original);
bool string_is_same(const char* string1, const char* string2);
void my_exit(int status);
